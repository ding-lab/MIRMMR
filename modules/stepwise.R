#Module returns best model using stepwise regression
msi_column
first_parameter
direction (default="both")
require(MASS)
if(msi_column is binary){
  write model statement
  evalute entire model
  run stepAIC()
  return best model and predicted values
}

